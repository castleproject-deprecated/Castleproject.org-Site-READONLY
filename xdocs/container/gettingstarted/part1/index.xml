<?xml version="1.0"?>
<document order="1">
  <properties>
    <title>Part 1 - The basics</title>
  </properties>

  <body>

<intro>



<p>
In this part of the Getting Started you will be introduced
to the basic Windsor Container operations. You might be wondered: what
about Castle MicroKernel? It is enough to say that when you are
using the Windsor Container, you are, by consequence, using the MicroKernel
as well.
</p>

<p>
We will use a Winforms project to experiment with the Windsor container.
To set it up, follow the steps:

<ol>

<li>
<p>Open Visual Studio and go to New\Project... Select <tt>Windows Application</tt></p>

<p>
<img relative="images/container/gs_part1_1.png" />
</p>

</li>

<li>
<p>
Now create a class named <tt>App</tt>. We will use it as the application entry point:
</p>

<pre format="cs">
namespace GettingStartedPart1
{
	using System;
	using System.Windows.Forms;

	public class App
	{
		public static void Main()
		{
			Application.Run(new Form1());
		}
	}
}</pre>

</li>

<li>
Remove the entry point method from the <tt>Form1.cs</tt> generated for you by Visual Studio:

<pre format="cs">
namespace GettingStartedPart1
{
	using System;
	using System.Windows.Forms;

	public class Form1 : System.Windows.Forms.Form
	{
		...
		
//		[STAThread]
//		static void Main() 
//		{
//			Application.Run(new Form1());
//		}
	}
}</pre>

</li>

</ol>

</p>

<p>
You can also download the complete example:

<download 
	relative="!download/container/v1rc3/GettingStartedPart1.zip" 
	filename="GettingStartedPart1.zip"/>

</p>


<p>
Proceed with <a href="requirements.html">Requirements</a>.
</p>


</intro>

</body>
</document>
